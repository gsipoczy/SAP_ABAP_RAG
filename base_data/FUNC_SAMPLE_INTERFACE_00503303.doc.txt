DE-EN-LANG-SWITCH-NO-TRANSLATION
DE-EN-LANG-SWITCH-NO-TRANSLATION
DE-EN-LANG-SWITCH-NO-TRANSLATION
DE-EN-LANG-SWITCH-NO-TRANSLATION
DEFINE &TEXT& = 'Maintain conditions: Transitions'
DEFINE &EVENT& = '00503303'
DEFINE &TYPE& = 'INTERFACE'
INCLUDE FI_OPEN_FI_DOCUMENTATION_1 OBJECT DOKU ID TX
&FUNCTIONALITY&
This function module (user-exit) allows you to decide which field
contents are permitted in condition maintenance when creating or
changing condition records.

The following import parameters are available:

KOMG_OLD             ,,old status of KOMG structure

KOMG_NEW             ,,new status of KOMG structure

VAKE_OLD             ,,old status of VAKE structure

VAKE_NEW             ,,new status of VAKE structure

You can set the export parameter TRANSITION_FORBIDDEN to either
SPACE or 'X':

If you set it to SPACE, you can continue with condition maintenance.

However, if it is set to 'X', then you will receive an error message.

&EXAMPLE&
To display your workflow, you can use the processing status for the
condition records, for example. This functionality is available from
Release 4.6A for newly created condition tables, as long as the setting
was made accordingly in the condition table.

If your workflow does not allow, for example, the transition from
processing status from 'A' to 'C' (as only 'A' to 'B' or 'B' to 'C' is
permitted), you can compare the two fields
KOMG_OLD-KBSTAT
KOMG_NEW-KBSTAT
in this user exit.

&HINTS&
SAP cannot ensure that all fields in the KOMG and VAKE structures have
been completed with the appropriate values. To implement this user
exit, we urgently recommend that you check the current content of the
fields that you are using.
